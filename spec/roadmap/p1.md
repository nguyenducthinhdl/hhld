# P1 — Core interfaces

- **Status:** done
- **Goal:** Define `Exchange`, Strategy, Risk, PnL (and related) interfaces only. `Exchange` must fit crypto books and prediction-market venues. Strategy/Risk/PnL must allow a multi-leg hedge later without breaking crypto-only arb.
- **Done when:** compile-time contracts exist; no real venue or market data.
- **Skip:** implementations beyond stubs needed to compile; no Polymarket adapter or hedge strategy yet.

## Delivered

- `src/exchange`: types (`Book`, `Tick`, `OrderRequest`, `Fill`, `Kind` incl. prediction) + `Exchange` interface
- `src/strategy`: `Leg`, `Decision` (`TraceID` / `HedgeID`) + `Strategy` interface
- `src/risk`: `Verdict` + `Risk.Evaluate` on whole `Decision`
- `src/pnl`: `Tracker` with `SnapshotByHedge`
- `src/sim`: `Simulator` interface
- `src/warehouse`: `Store` interface
- `src/admin`: `Auditor` with order records + PnL by hedge
- **Verification / unit tests** (`go test ./...`):
  - Compile-time `var _ Interface = (*stub)(nil)` checks per package
  - Stub unit tests for exchange, strategy, risk, pnl, sim, warehouse, admin
  - Cross-contract test: Decision → Risk → `OrderRequest` legs (`src/contract_test.go`)
- `go build ./...` and `go vet ./...` clean; no adapters or market I/O

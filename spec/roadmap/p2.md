# P2 — Fake tick feed

- **Status:** done
- **Goal:** Deterministic dual-venue fake ticks + clock for tests.
- **Done when:** fake books/ticks drive a consumer without network I/O.
- **Skip:** crawl, warehouse, live websockets.

## Delivered

- `src/exchange/clock.go`: `Clock`, `WallClock`, `ManualClock` (Set/Advance) for deterministic time
- `src/exchange/fake`: in-memory `Exchange` (`SetBook`, `PushTick`, paper Place/Cancel) — no network
- Latency simulation: `SetBookDelay` / `SetOrderDelay` (wall-clock sleep; respects ctx deadline → timeout)
- `fake.NewDual`: two venues sharing one `ManualClock` for arb tests
- Tests: dual-book consumer, tick push, paper order clock, subscribe delivers existing book, book/order delay + timeout
- `go test ./...` passes

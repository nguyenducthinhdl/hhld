# P4 — Risk gates (miss-more doctrine)

- **Status:** pending
- **Goal:** Reject trades that do not survive worst-case fees, latency, and partial-fill assumptions. Include venue/book health and staleness per [networking.md](../networking.md). Enforce per-hedge (or arb-key) serial evaluation and global in-flight caps per [concurrency.md](../concurrency.md).
- **Done when:** risk module blocks negative-edge paper trades; preferred behavior is miss opportunities; stale/unhealthy books reject the Decision; concurrent Decisions for the same hedge/arb key cannot interleave Risk; overload misses instead of unbounded queues.
- **Skip:** live capital, exchange-specific quirks beyond modeled costs; full unpaired-leg recovery playbook (harden in P10).

## Delivered

_(fill when Status becomes done)_

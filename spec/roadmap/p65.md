# P6.5 — Crawl replay simulation UI

- **Status:** done
- **Goal:** Replay crawled NDJSON for **any two venues** + one symbol through paper arb; editable config tables re-run the sim; D3 chart of gap, PnL, and signals.
- **Done when:** `go run ./cmd/hhld-sim -ndjson data/samples/btcusd_books.ndjson` serves `GET /sim` with series JSON, POST overlay, hover price×size, time/y-scale scrollers.
- **Skip:** writing config back to disk, live capital, TradingView.

## Delivered

- [`src/sim`](../../src/sim/): `InputFromNDJSON`, `Trace` / `TracePair`, `ApplyOverlay`; each step includes `explain` (gap formula, buy/sell venue, size, fee/commission).
- [`src/risk`](../../src/risk/): `ExplainDecision` / `ExplainQuote` for miss-more economics.
- Chart hover: formula `gap = sell_bid(venue) - buy_ask(venue)`, volume, per-leg rate/fixed/commission, gross − fee − latency = net.
- [`src/admin`](../../src/admin/): `GET /sim`, `POST /sim/run`.
- [`cmd/hhld-sim`](../../cmd/hhld-sim/): `-ndjson`, `-config`, `-venue-a`, `-venue-b`.

```bash
go run ./cmd/hhld-sim -ndjson data/crawl/btcusd-live.ndjson
# → http://127.0.0.1:8080/sim
```

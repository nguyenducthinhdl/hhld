# P8.5 — Event-driven BookStore

- **Status:** done
- **Goal:** In-process market events + BookStore (snapshot/delta) + Runner that re-evaluates Strategy on every book update from either venue when both sides are ready.
- **Done when:** deltas apply correctly; Runner triggers `OnBooks` on each venue update once the peer book exists; Risk `TryAcquire` still serializes; sim path unchanged.
- **Skip:** live capital, coalesce windows, external brokers, Strategy consuming deltas directly, full P9 live paper loop.

## Delivered

- [`src/market`](../../src/market/): `BookEvent` / `BookDelta`, `BookStore` (snapshot replace, delta merge, size `"0"` delete, reject delta-before-snapshot and backward seq), bounded `Bus`, `Runner` (evaluate on every update when both venues ready → Strategy → Gate → paper place → audit).
- Bridges: `BridgeBooks` (HL / GRVT snapshot feeds), `BridgeGRVTDeltas` (`v1.book.d` + Clear on reconnect), `BridgeFakeDeltas` / `fake.PushDelta`.
- GRVT: `SubscribeBookDeltas` for `v1.book.d`; HL `l2Book` documented as snapshot-only events.
- Specs: trading (event-driven section), concurrency (in-process bus), QA catalog rows.
- Tests: store merge/reject, runner peer-miss + every-update + lock_busy under burst, GRVT delta WS, fake/bridge paths.

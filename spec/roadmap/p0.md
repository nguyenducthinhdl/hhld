# P0 — Repo skeleton

- **Status:** done
- **Goal:** Go module layout, package stubs, constitution linked from README.
- **Done when:** `go` packages exist as skeleton dirs; `spec/` is discoverable.
- **Skip:** adapters, strategies, infra.

## Delivered

- Module `github.com/nguyenducthinhdl/hhld` (`go.mod`, `.gitignore`)
- Package stubs under `src/`: `exchange`, `strategy`, `risk`, `pnl`, `sim`, `warehouse`, `admin`
- README layout section + constitution links; `go build ./...` succeeds

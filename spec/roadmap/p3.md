# P3 — Arb strategy (paper)

- **Status:** done
- **Goal:** Detect gaps on fake dual-venue books; emit paper buy/sell decisions.
- **Done when:** simulated arb produces paper orders for multi-symbol config (BTCUSD first).
- **Skip:** fee/latency gates (next phase), live feeds.

## Delivered

- `src/strategy/arb.go`: `CrossVenueArb` — multi-symbol config, buy-low/sell-high two-leg `Decision` (BTCUSD first)
- `src/strategy/paper.go`: `PlaceDecision` — concurrent paper place of all legs; returns per-leg results on partial failure
- `src/config` + `strategy.ArbConfigFrom`: symbols / trading conditions / venues / timeouts parameterized (see also later config submodule note)
- Unit tests (`src/strategy/paper_test.go`):
  - Happy path: gap → decision → both legs accepted
  - **1-leg order timeout** (one venue delayed)
  - **2-leg order timeout** (both venues delayed)
  - **2-leg order delay success** (delay under deadline)
  - **1-leg book timeout** / **2-leg book timeout** on snapshot
- `go test ./src/strategy/` passes

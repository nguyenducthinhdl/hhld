# P6 — Backtest simulation

- **Status:** pending
- **Goal:** Replay ticks/books through the same Strategy/Risk path → PnL series. Provide abstracted **winning rate** and **winning distribution** `(symbol, gap, volume1, volume2, exchange1, exchange2, time)` for Risk calibration (formula/empirical in Go; ML in research side project).
- **Done when:** `Simulator.Run` accepts market input + strategy + risk → PnL; `Analyzer` can report WinningRate and WinningDistribution (even if first impl is empirical over the run).
- **Skip:** rich Visualization Module (CSV is enough); ML training (side project).

## Delivered

_(fill when Status becomes done)_

## Interface hooks (already in tree)

- `sim.Simulator`, `sim.Analyzer`, `sim.OutcomeDims` / `Distribution`
- `risk.Estimator`, `risk.Manager`, `risk.FormulaEstimator` (neutral prior until sim-calibrated)
